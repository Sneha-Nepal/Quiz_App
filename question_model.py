class Question:

    def __init__(self, text, answer, options):
        """Constructor method with text, correct answer, and multiple-choice options."""
        self.text = text
        self.answer = answer
        self.options = options
        